# RELEASE_NOTES

友易行可交付版本说明。每个对内或对外交付版本都必须记录版本号、提交、主要内容、已知问题和回退方式。

## closing-sprint-quote-data-2026-07-03

发布日期：2026-07-03

版本类型：客户测试结单冲刺补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 报价明细来源说明瘦身：默认显示短状态，完整候选和失败原因进入折叠详情 / 诊断面板。
- 新增报价诊断快照 `youyixing_quote_diagnostics_latest`，便于回溯每行服务、城市、项目、状态、成本、来源和原因。
- 图片/PDF 导出依赖本地化，避免客户网络无法访问 CDN 时导出库加载失败。
- 云端用车匹配路线评分修复：有明确路线时，路线不命中不能直接判定 `matched`。
- 新增报价展示和产品资源路线评分回归测试。

### 已验证

- `node --check app.js` 通过。
- `node --check server.js` 通过。
- `node --test tests/*.test.js` 31 项通过。
- 本地服务 `http://127.0.0.1:8787/` 返回 `HTTP/1.1 200 OK`。
- 浏览器 smoke check：`html2canvas`、`jsPDF`、`YouyixingQuoteDisplay` 均加载成功。
- 重庆接机 / 送机 7 座成本 250；重庆武隆包车 14座~17座成本 1500；重庆市内一日游 8 小时 7 座成本 700。
- 当前云端产品库严格审计：用车 940、导游 48、门票 145、酒店 153、餐厅 151、特色体验 77、线路产品 24；历史三类脏字段命中 0。

### 本版不包含

- 不重写报价主流程。
- 不新增订单、权限、飞书或老板看板。
- 不自动补齐餐厅真实成本。
- 不承诺客户方案图片/PDF 文件已在每种浏览器和公网环境实点下载完成。

### 已知风险

- 餐厅 151 条当前缺真实成本字段，报价中应继续显示待补成本。
- 门票仍有 13 条缺成本，酒店 5 条缺成本，特色体验 2 条缺成本。
- 跨城主行程城市错位问题仍需用成都送机 / 重庆接机真实需求单独验收。
- 运行日志仍不足以完整复盘 AI 原始输出和 fallback 原因。

### 回退方式

```bash
git revert <本次提交哈希>
```

如只需临时停用导出本地依赖，可把 `index.html` 中 `html2canvas/jspdf` script 改回 CDN；如只需回退云端路线评分，可恢复 `server.js` 的旧 `productResourceMatchScore()`。

## domain-agent-foundation-2026-07-03

发布日期：2026-07-03

版本类型：内部架构安全补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 新增当前架构审计文档、P0/P1 改造计划、报价引擎契约、P1 数据库目标模型和老板看板事件模型。
- 新增报价 domain 纯函数层：需求归一化、候选生成、报价行生成、报价版本汇总和警告。
- 新增产品备注结构化模块：`normalizeProductRemarks(product)`。
- 新增 Agent 工具注册、权限、执行网关和日志骨架。
- 新增领域事件创建函数。
- 新增旧报价流程兼容 adapter。
- 新增 5 个 Node 单测。

### 已验证

- `node --check app.js` 通过。
- `node --check server.js` 通过。
- `public/js/**` 全量语法检查通过。
- `node --test tests/*.test.js` 26 项通过。

### 本版不包含

- 不重写 `app.js`。
- 不替换现有报价主流程。
- 不新增真实 Agent 模型工具调用。
- 不新增老板看板页面。
- 不迁移供应商服务明细到云端。
- 不修复客户方案图片/PDF 下载。

### 已知风险

- 新 quote domain 当前只是可测试边界，后续接入 `buildQuote` 时必须逐品类小步切换。
- Agent Gateway 当前是前端/测试骨架，不能当作正式权限系统。
- 领域事件当前只创建对象，不写数据库。

### 回退方式

```bash
git revert <本次提交哈希>
```

如只需临时停用新增模块，可删除 `index.html` 中 `public/js/**` 的新增脚本引入。

## cloud-product-ui-match-2026-07-03

发布日期：2026-07-03

版本类型：内部验收补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 产品资源库页面读取 Supabase 最新发布产品库批次。
- 报价资源池由云端产品库生成，报价行保留云端资源 UUID、供应商、来源和匹配状态。
- 景点门票页面恢复专用字段：类型、票种、淡季成人、旺季成人、旅行社成人、免费政策、保票政策。
- 云端产品库启用时忽略旧本地产品库覆盖层，避免坏缓存污染报价。
- 修复门票正式名和简称重复生成报价项的问题。
- 新增本轮验收报告：`docs/acceptance-cloud-product-ui-match-20260703.md`。

### 已验证

- 云端产品库数量：用车 940、特色体验 77、门票 145、导游 48、酒店 153、餐厅 151。
- 重庆接机 / 送机 7 座成本 250。
- 重庆武隆包车 14 座成本 1500。
- 重庆市内一日游 8 小时 7 座成本 700。
- 北京大兴机场接机成本 260。
- 北京市内用车成本 700，并在多个候选时标记 `need_confirm`。
- 北京英文导游成本 860。
- 故宫博物院门票成人 60、儿童 30。
- 北京酒店返回同城同星级候选。
- 餐厅缺成本显示待补成本，不显示 0。

### 本版不包含

- 不重写报价主流程。
- 不新增订单、权限、飞书同步或 SaaS 模块。
- 不把供应商服务明细迁入云端。
- 不修复客户方案图片/PDF 下载。

### 已知风险

- 餐厅源表缺人均最低成本，当前 151 条餐厅全部为待补成本。
- 供应商服务明细仍在本地状态，后续需要单独入云端并复测成本覆盖。
- 客户方案图片和 PDF 下载仍需单独修复。

### 回退方式

```bash
git revert <本次提交哈希>
```

如需临时停用云端产品库，可删除本机 `.env.supabase.local`，前端会保留静态系统产品库 fallback。

## supabase-product-catalog-foundation-2026-07-03

发布日期：2026-07-03

版本类型：内部数据底座补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 新增 Supabase Postgres 产品库表结构。
- 新增云端产品库导入脚本和质量门禁。
- 已发布产品库批次：`4d50f5cc-d8ff-47ff-89bd-1028056814b3`。
- 新增服务端产品库查询和匹配 API。
- 修复导游、特色体验、门票空白行继承生成假资源的问题。
- 当前有效产品数量：线路 24、用车 940、导游 48、特色体验 77、门票 145、餐厅 151、酒店 153。

### 本版不包含

- 不重写报价主流程。
- 不把产品库页面和报价主流程一次性全部切到 Supabase。
- 不新增订单、权限、飞书同步或 SaaS 模块。
- 不修复客户方案图片/PDF 下载。

### 已知风险

- 前端产品库页面和报价匹配仍需下一轮逐步切到服务端 API。
- 餐厅 Sheet 的人均最低成本列在当前 Excel 中为空，151 条餐厅全部保留为待补成本。
- 供应商服务明细后续需要再导入云端并接入报价行。

### 回退方式

```bash
git revert <本次提交哈希>
```

如需临时停用云端 API，删除本机 `.env.supabase.local` 或停用相关环境变量即可；Supabase 中保留历史导入批次，不需要删除数据。

## product-catalog-real-excel-2026-07-03

发布日期：2026-07-03

版本类型：内部验收补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 用真实《产品库汇总.xlsx》重建系统产品库。
- 修复特色体验和门票有效行因票种为空被跳过的问题。
- 修复餐厅 K/L 列和特色体验 M 列备注保存错位。
- `rawFields` 增加 Excel 列坐标和多行表头组合字段。
- 新增真实产品库导入验收报告和回归测试。

### 本版不包含

- 不重写报价主流程。
- 不新增订单、权限、飞书同步或 SaaS 模块。
- 不修复客户方案图片/PDF 下载。

### 已知风险

- 浏览器端完整报价明细仍需用重庆和北京客户案例继续手工验收。
- 餐厅 Sheet 的人均最低成本列在当前 Excel 中为空，系统保留为待补成本，不自动用建议卖价当成本。

### 回退方式

```bash
git revert <本次提交哈希>
```

## supplier-v1.4-quotable-resource-2026-07-03

发布日期：2026-07-03

版本类型：内部验收补丁

对应提交：见 `git log -1 --oneline`

### 本版包含

- 新增可报价资源 core。
- 新增浏览器内可报价资源查询 service。
- 新增产品资源与供应商服务明细关联层。
- 新增报价行从资源库选择供应商资源。
- 新增报价行成本快照。
- 新增供应商状态、成本、参考价变更本地操作日志。
- 新增 V1.4 数据流和验收文档。
- 新增 Node 回归测试。

### 本版不包含

- 不重写报价主流程。
- 不新增完整订单系统。
- 不新增完整财务付款系统。
- 不新增真实数据库。
- 不修复客户方案图片/PDF 下载问题。

### 已知风险

- 当前查询 service 运行在浏览器本地，server.js 无法直接读取页面 localStorage。
- 自动报价匹配仍沿用旧主流程，本轮补的是供应商资源选择和快照。
- 真实 Excel 导入后仍需继续手动验收产品资源关联与报价行选择。

### 回退方式

```bash
git revert <本次提交哈希>
```

## v0.1.0-baseline-2026-07-02

发布日期：2026-07-02

版本类型：baseline / 内部审查基准

对应 tag：`v0.1.0-baseline-2026-07-02`

对应 commit：`49b2624`

提交信息：`chore: create baseline version for Youyixing project`

### 本版包含

- 报价项目工作台。
- 客户需求输入和小易识别。
- 主行程表、线路生成、线路编辑。
- 产品资源库、产品导入、产品筛选、产品去重、产品导出。
- 供应商管理、供应商导入导出、服务明细。
- 报价明细、成本汇总、缺成本提示。
- 客户方案、多语言、图片/PDF 导出入口。
- AI 模型配置和报价规则设置。
- 当前本地数据、产品库快照、验收报告文件。

### 本版不承诺

- 不承诺产品库导入和报价匹配已全部通过真实 Excel 回归。
- 不承诺图片和 PDF 导出在所有环境可用。
- 不承诺公网静态页具备真实 AI 后端能力。
- 不承诺订单、财务、权限、飞书同步为完整功能。

### 已知风险

- 产品库导入、产品匹配、供应商成本回填仍是 P0 风险。
- 运行日志不足以完整审计 AI 识别过程。
- 静态公网和本地服务能力不同，客户测试需区分环境。
- `app.js` 体量很大，后续修复必须小步提交，避免再次不可追踪。

### 回退方式

只查看 baseline：

```bash
git switch --detach v0.1.0-baseline-2026-07-02
```

把当前分支回退到 baseline：

```bash
git reset --hard v0.1.0-baseline-2026-07-02
```

执行硬回退前必须确认没有未保存改动。

## docs-management-2026-07-02

发布日期：2026-07-02

版本类型：内部开发管理补丁

对应提交：待提交后补充

### 本版包含

- 新增 `docs/DEV_LOG.md`。
- 新增 `docs/CHANGELOG.md`。
- 新增 `docs/BUG_LOG.md`。
- 新增 `docs/FEATURE_MAP.md`。
- 新增 `docs/QA_CHECKLIST.md`。
- 新增 `docs/RELEASE_NOTES.md`。
- 新增 `docs/VERSION_ARCHIVE.md`，记录历史压缩包和相关开发资产。

### 本版不包含

- 不修改业务逻辑。
- 不修复产品库、报价、导出或 AI 相关 bug。
- 不新增业务模块。
