# BUG_LOG

友易行 bug 台账。所有客户反馈、自测发现、开发过程发现的问题都必须登记。

## Bug 记录模板

Bug 编号：

发现日期：

发现来源：客户反馈 / 自测 / 开发过程 / 其他

问题描述：

出现位置：

复现步骤：

预期结果：

实际结果：

严重程度：高 / 中 / 低

影响范围：

可能原因：

修复方案：

涉及文件：

验证步骤：

验证结果：

当前状态：待修复 / 修复中 / 已修复 / 待确认 / 暂缓

---

## 当前已知问题登记

### BUG-20260702-001

Bug 编号：BUG-20260702-001

发现日期：2026-07-02

发现来源：客户反馈 / 开发过程

问题描述：产品库多 Sheet Excel 导入、品类归属、成本字段、供应商字段和报价明细回填曾多次出现不稳定，用户反馈过用车、酒店、导游、门票等资源被放错类目，成本为空或显示 0，报价匹配失败。

出现位置：产品资源库导入弹窗、产品库列表、报价明细生成。

复现步骤：

1. 清空或重置产品库。
2. 导入《产品库汇总.xlsx》。
3. 查看各 Sheet 是否进入正确品类。
4. 生成重庆接送机、重庆包车、北京接机、北京酒店、导游和门票报价。

预期结果：各 Sheet 进入正确品类，空成本显示待补成本，能按产品库成本进入报价明细。

实际结果：历史反馈显示曾出现脏字段、错类目、空成本、0 成本、匹配失败。

严重程度：高

影响范围：报价成本、产品库可信度、客户测试交付。

可能原因：多轮修复叠加，导入适配器、通用导入、产品标准化、持久化覆盖层和报价匹配之间仍需完整回归。

修复方案：下一轮只围绕产品库导入与报价成本回填做复现、最小补丁和对抗性验收。

涉及文件：`app.js`、`data/products/youyixing-product-catalog.json`、`vendor/xlsx.full.min.js`

验证步骤：按 `docs/QA_CHECKLIST.md` 的产品库导入和报价匹配专项执行。

验证结果：待验证

当前状态：待确认

### BUG-20260702-002

Bug 编号：BUG-20260702-002

发现日期：2026-07-02

发现来源：客户反馈 / 自测

问题描述：非北京城市需求曾被识别成北京默认线路，例如南京 3 天、洛阳 2 天、青岛 4 天被生成北京线路。

出现位置：需求识别、线路生成、主行程表。

复现步骤：

1. 输入 `4人，南京3天，需要用车和英文导游，舒适型酒店`。
2. 点击生成报价草稿。
3. 查看城市、天数和主行程表。

预期结果：生成南京 3 天线路。

实际结果：历史反馈显示曾生成北京线路。

严重程度：高

影响范围：行程识别、报价城市、产品匹配。

可能原因：城市词表不足、中文天数解析失败、AI 返回结构不合规时仍被当成成功、空城市兜底北京。

修复方案：baseline 前已做热修，扩大城市词表，修复中文天数解析，增加 AI schema 防护，取消北京兜底。

涉及文件：`app.js`

验证步骤：

1. 测试南京 3 天。
2. 测试洛阳 2 天。
3. 测试青岛 4 天。
4. 测试重庆 1 天。

验证结果：本地和公网曾验证通过，但后续修改仍需保留回归项。

当前状态：已修复

### BUG-20260703-001

Bug 编号：BUG-20260703-001

发现日期：2026-07-03

发现来源：客户反馈 / 开发过程

问题描述：供应商管理 V1.3 验收时，供应商服务明细虽然能显示在供应商页面，但缺少稳定的可报价资源出口，报价页无法按产品资源和供应商服务明细选择并写入成本快照。

出现位置：供应商管理、产品资源库、报价明细。

复现步骤：

1. 维护一个启用供应商和服务明细。
2. 进入产品资源库查看对应产品。
3. 进入报价明细尝试选择该供应商服务明细。
4. 查看报价行是否保留供应商、服务明细、成本来源和成本快照。

预期结果：供应商服务明细可生成可报价资源，报价行选择后写入成本、供应商和快照字段。

实际结果：历史版本只有旧 `quoteResources` 混合资源，没有独立查询 service 和 `quoteLineSnapshot`。

严重程度：高

影响范围：供应商成本回填、报价可追溯性、V1.4 验收。

可能原因：供应商服务明细直接混入旧报价资源，缺少 `product_resource -> supplier_service_detail -> quotable_resource -> quote_line` 的稳定出口。

修复方案：新增 `quotable-resource-core.js`、`state.quotableResources`、`window.YouyixingServices.queryQuotableResources`，并在报价行新增“从资源库选择供应商资源”。

涉及文件：`quotable-resource-core.js`、`app.js`、`index.html`、`styles.css`、`tests/quotable-resource-core.test.js`

验证步骤：

1. 执行 `node --check app.js`。
2. 执行 `node --check quotable-resource-core.js`。
3. 执行 `node --test tests/quotable-resource-core.test.js`。
4. 启动本地服务并打开页面。

验证结果：语法检查通过，核心测试 6 项通过，本地页面返回 200。

当前状态：已修复

### BUG-20260703-002

Bug 编号：BUG-20260703-002

发现日期：2026-07-03

发现来源：开发过程

问题描述：页面底部直接执行 `init(); writeAcceptanceProbe();`，异步初始化未完成时验收探针可能读取空产品库或空质量报告，导致启动期控制台风险。

出现位置：`app.js` 启动尾部。

复现步骤：

1. 打开本地页面。
2. 观察初始化期间控制台和验收探针。

预期结果：验收探针在初始化完成后执行。

实际结果：旧写法没有等待 `init()` 完成。

严重程度：中

影响范围：页面启动、验收探针、控制台报错。

可能原因：异步函数未等待。

修复方案：改为 `init().then(writeAcceptanceProbe).catch(...)`。

涉及文件：`app.js`

验证步骤：打开 `http://127.0.0.1:8799/`，检查页面加载和控制台 error。

验证结果：本地浏览器烟测业务控制台无 error。

当前状态：已修复

### BUG-20260702-003

Bug 编号：BUG-20260702-003

发现日期：2026-07-02

发现来源：客户反馈

问题描述：客户方案生成后，下载图片和下载 PDF 曾反馈无法使用。

出现位置：客户方案页、导出图片按钮、下载 PDF 按钮。

复现步骤：

1. 完成客户需求、线路和报价。
2. 生成客户方案。
3. 点击导出图片。
4. 点击下载 PDF。

预期结果：浏览器下载图片或 PDF 文件。

实际结果：客户反馈完全下载不了。

严重程度：中

影响范围：客户交付物导出。

可能原因：`html2canvas` 或 `jspdf` 依赖、静态环境加载、方案确认状态、浏览器权限、DOM 渲染状态均可能相关。

修复方案：待复现后单独修复，不和产品库修复混在一起。

涉及文件：`index.html`、`app.js`

验证步骤：使用真实报价项目生成方案后，在本地服务和公网预览分别点击导出图片、下载 PDF。

验证结果：待验证

当前状态：待修复

### BUG-20260702-004

Bug 编号：BUG-20260702-004

发现日期：2026-07-02

发现来源：开发过程

问题描述：当前 `data/agent-operation-log.json` 只记录少量 action，缺少原始客户需求、AI 原始输出、归一化结果、fallback 原因、主行程天数和最终城市，导致“为什么识别错”难以复盘。

出现位置：AI Agent 请求、需求识别、线路生成、运行日志。

复现步骤：

1. 多次输入不同城市需求并生成草稿。
2. 查看 `data/agent-operation-log.json`。
3. 尝试从日志复盘模型输出和系统 fallback。

预期结果：日志能复盘输入、输出、归一化和回退原因。

实际结果：日志信息不足。

严重程度：中

影响范围：客户测试排障、AI 识别问题复盘。

可能原因：日志设计只记录 agent action，不记录关键中间态。

修复方案：后续增加轻量运行日志，不改变业务流程。

涉及文件：`server.js`、`app.js`、`data/agent-operation-log.json`

验证步骤：输入南京、洛阳、重庆需求，确认日志能看出每次输入、输出、最终城市和 fallback 原因。

验证结果：待验证

当前状态：待修复
